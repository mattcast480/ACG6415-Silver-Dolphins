# Development-only dependencies — NOT required to run the app.
# Install with:  pip install -r requirements-dev.txt

# (pdfplumber moved to requirements.txt — it is now a runtime dependency used by
# code_table_loader.py to parse PDF files in 1.code_tables/ at startup.)
